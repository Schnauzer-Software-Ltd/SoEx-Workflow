<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>PiiMaker.Access.Identity.Interface</RootNamespace>
    <AssemblyName>PiiMaker.Access.Identity.Interface</AssemblyName>
  </PropertyGroup>

  <!-- The Identity access-component CONTRACT, in its own assembly (<Company>.<Tier>.<Component>.Interface) —
       the component-per-assembly convention. A plain contract: no SoEx dependency. The service implements it
       in PiiMaker.Access.Identity.Service. -->

</Project>
